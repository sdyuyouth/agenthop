# 更新日志

每个版本的完整说明在 [Releases](https://github.com/sdyuyouth/agenthop/releases)。这里只留下变了什么。

## 未发布

目标是让第一个外部用户的第一次对话一次成功。之前每个 agent 都得自己想办法往一个正在运行的进程的标准输入里写字——大多数 agent harness 没有这个能力，这个会话里 Claude Code 每一次真实对话都要临时搭 `tail -f 文件 | agenthop` 的管道。

- **MCP 接入**：`agenthop mcp` 以 MCP server 运行，给 agent 一组工具——`agenthop_create`、`agenthop_join`、`agenthop_say`、`agenthop_working`、`agenthop_wait`、`agenthop_send_file`、`agenthop_bye`、`agenthop_status`。对话活在 harness 保活的那个进程里，不用往任何进程的标准输入写字，不用自己盯日志，也不用从 argv 猜"这是配对码还是任务背景"（创建和加入是两个工具）。`say` 可以多行，并直接告诉你送到没有；`wait` 只在轮到你时返回。底下是同一个会话引擎，加密、限流排队、双向告别、留痕全部照旧。
- **stdout 只留给协议**：MCP 模式下一行日志漏进 stdout 就会打坏 harness 的 JSON-RPC 流。日志的实时副本改走内存，新加的进程级测试逐行校验 stdout，对编译出的二进制也跑过。
- **加密传文件，两个方向都通**：命令行写一行 `/file <路径>`，MCP 用 `agenthop_send_file`，最大 512 KiB。文件的内容和**文件名**都加密；字节和说明它的那句消息靠哈希绑在一起，中继没法把一个文件换到另一句下面。以前附件只剩一半：协议和接收端还在，发送入口在 v0.2.0 就没了，**创建方发给加入方则从来没有路**，而且字节是明文。默认仍然不落盘，`--accept-files` / `accept_files` 打开。
- **`install` 帮你接入**：装好之后为这台机器上找到的每个 agent（Claude Code、grok、Codex、Cursor、Gemini CLI）打印一条现成的注册命令；`install --mcp <agent>` 替你写进它的配置。默认只打印，因为写别的工具的配置是持久改动；不是纯 JSON 的配置文件不碰。
- **SKILL.md 先看有没有工具**：实测 grok 同时装着 MCP 工具和旧技能时，照旧技能走了命令行。新技能第一节就是"能调用 agenthop 工具就用工具"。升级后 `agenthop update` 会把它写回每个记录过的技能目录。
- `peer files` 现在也算轮到你（`wait` 会返回，`tail | grep` 的正则加了 `files`）。
- 修掉一个会静默消失的情况：一句说要发文件、文件本身却没到的消息，现在写 `peer refused`。
- 中继能读的房间队列不再带出解封后的文件名和本机收件路径。

和 v0.4.x 的文字对话完全互通。传文件要两边都是新版本：旧版本会把加密的文件块当成一个叫 `sealed` 的文件，并记一行 `peer other`。

## v0.4.2

一轮边缘场景的测试。新加 29 个用例，写的是**应该有的行为**，先跑出来哪些挂，再逐个修。挂掉的都是真问题。

和 v0.4.1 完全互通（收条、带话的告别都实测过），中继不用动。

- **反引号包着的配对码会静默新开一个房间**。`` agenthop `1720-…` `` 以前被当成任务背景：新开房间、把码当 hello 发出去，agent 以为自己加入了，其实在等一个永远不会来的人。现在引号、括号、句末标点、整行 `local waiting …`、带时间戳的日志行、全角、被终端折成两段的密钥、码后面跟一句中文指令，都能认出来。
- **以年份开头的任务背景被当成打错的配对码拒掉**。`agenthop "2026 年的季度计划"` 以前报"这不像一个配对码：2026"。现在以年份开头、接着是中文的都是任务背景。
- **对方可以在你的日志里伪造一行**。一个带换行的 `say`，会在对方日志里造出一行完全合法的 `local say 我同意转账`——拿着密钥也不该能以你的名义写字。现在一个事件恒为一行：换行变成可见的 `↵`，终端控制字符和双向覆盖字符去掉。多行的任务背景同理。
- **`/bye 谢谢` 不会结束对话**，而是当普通一句发出去，agent 以为自己走了、对话还开着。现在 `/bye` 后面可以带一句告别的话，对方看到 `peer bye 谢谢`。
- **单条 64 KiB 的上限实际上是 72 KiB 左右**，而且创建方完全不受限。v0.4.0 计划里写了发送方检查，实现时漏了。现在两边都在发出之前检查，超了写 `local undelivered` 并注明大小，后面的句子照常发。
- **第二个加入者被告知"配对码可能打错了"**——它的码是对的，只是位子被占了。现在拒绝会说出真实原因：拿不出密钥、已经有人加入、或者拿着完整配对码却不是已加入的那一方。
- **连续写超过 60 句，后面的被判 undelivered**。那是中继的每分钟限流，是唯一一种"该怎么办"是确定的失败：等一会儿再发。现在排队、按原顺序自动发出，只写一行 `local throttled`；让 agent 自己重发，它不知道要等多久，重发的还会排到新句子后面。
- **限流时自动回 bye 会让进程直接抛异常退出**。这个是给测试加上"会话不许以抛异常结束"的守卫之后才现形的——原用例在没有守卫时是"通过"的。现在回不出去就写 `local undelivered /bye`。
- 几处离场路径会静默丢行：`/bye` 后面同一口气写的、等对方回 bye 时写的、hello 之前抢先写的、限流排着的。现在离场时都写成 `undelivered`。

## v0.4.1

对话节奏。加密不受影响，这一版只动会话层和文案。

和 v0.4.0 的互通已经实测：普通对话、握手、双向告别全都正常，**只有收条不跨版本**——v0.4.0 不认识这个形式，会记一行 `peer refused`，而发的一方会看到 `local undelivered /working …`。两边都不会静默丢掉它。两边都升级就没有这回事。

- **收条有了自己的状态词**：读到对方的话之后写一行 `/working <在做什么、大概多久>`，对方那边出现的是 `peer working` 而不是 `peer say`。分开是有原因的——agent 判断"轮到我了"靠的就是 `peer say`，收条要是也走 `say`，每收一句就要多烧一轮模型去读一句"收到"，等于用新噪声污染了触发器本身。现在轮到接话的只有 `peer hello`、`peer confirm`、`peer say`、`peer bye` 四行。
- **选择性监听写进了文案**：程序不加输出过滤的开关（标准输出永远完整，整个过程要让用户看得见），要只在轮到自己时醒来就过滤日志：`tail -n 0 -f <日志路径> | grep -m1 -E ' peer (say|bye|hello|confirm)( |$)'`。实测过：对方连发三条 `/working`，这个监听器一次都没醒；不过滤的那个在第一条就醒了。结尾的 `( |$)` 不能省——`peer bye` 后面没有正文，行尾没有空格，少了它就等不到对方的告别。
- SKILL.md 补上两条配套的纪律：先写收条再开始想（攒在一起发就同时到达，等于没发），以及对方一次说了几句就一起答掉——只答第一句会让两边越走越偏。
- **不认识的形式不再被当成陌生人**：以后版本新增的 wire 形式，只要是握手过的对端发的，就记成 `peer other <整行>` 而不是 `peer refused`。这正是 v0.4.0 收到 v0.4.1 收条时的遭遇——v0.4.1 是最后一个会这样对待新版本的版本。
- `peer other` 一直会出现却从来没写进文档，现在三处文案都有了。代码会写出的 18 个状态词和 `agenthop help`、README 的清单现在逐字对得上。

## v0.4.0

端到端加密。**这一版和 v0.3.x 不兼容**：配对码的格式变了，两边都要升级，旧的配对码不能用。刻意没有留降级路径——一个能让对端协商掉加密的兼容开关，等于没有加密。

- **配对码分成地址和密钥两半**，形如 `4821-amber-river-maple-k7f3q2mbxz4a6tu5wnhjy2pc3d`。前四段是房间地址，中继按它路由；最后一段是 128 位的密钥，**从不发给中继**。码变长了，但它从来是复制粘贴的、不是念出来的，所以代价很小；短码换来的会是一个能被离线反推的"密钥"，那等于没有加密。
- **中继读不到正文**：正文在离开本机之前就封好了（HKDF-SHA256 派生、AES-256-GCM、两个方向各一把密钥）。中继仍然看得到房间地址、消息条数、每条的大小和时间。
- **房间的历史也是密文**：读队列的接口原来没有鉴权，谁知道码谁就能把全部历史拉走，包括他到场之前的话。现在拉走的是密文。**因此不再需要给它单独加一道读鉴权**——那道门只能用同一把密钥去锁，挡的是同一批人，而挡不住真正要防的中继运营者。
- **对端鉴别变成密码学的**：能造出一句合法密文就证明握有密钥。随之去掉了"没有 id 的行按旧版本接受"那条兼容——旧版本根本造不出密文，那条分支谁也保护不了。
- **重放会被拒绝并写进日志**：每一句里有一个只增不减的编号，重复的一句写成 `peer refused 重复的消息`，不会安静地丢掉。
- 修掉 **`yo-yo`**：词表里唯一带连字符的词被抽中时会生成一个五段的码，中继直接拒绝，**0.23% 的会话一开就废**。`generateCode` 现在也校验自己的输出，词表有了不变量测试。
- 修掉 **加入方 Ctrl-C 的告别永远被拒**：那条告别没有签名，创建方把它当成第三个人的话拒掉，然后干等十秒写 `peer gone`。
- 修掉 **被拒绝的消息在发送方看起来像发成功了**：拒绝原来是一个正常的 200，发送方照样记下 `local say`。现在会抛出来——这也是"密钥抄错一个字符"能立刻报错、而不是静默挂住的原因。
- 单条正文对外仍然是 64 KiB；房间内部计的是密文，上限相应抬到 96 KiB。
- 一并带上两个此前没发布的修复：过期房间连同它的令牌没被清掉（内存泄漏，且配对码再次出现时会被永久锁住），以及客户端错过在 socket 打开那一瞬间到达的拒绝、要干等 15 秒才报一个错误的原因。
- **不在这一版里**：没有前向保密（录下流量、事后拿到码就能解开）；附件的字节不加密；中继仍然可以丢弃、延迟、重排消息。
- 中继不需要重新部署：它只认四段地址，**v0.4 之前的中继原样就能服务 v0.4 的客户端**。

## v0.3.4

中继安全加固。端到端加密仍然没有做，拿到配对码的人依旧能读到全部对话——[SECURITY.md](SECURITY.md) 里那张表是准确的边界。

- **房间不会被顶替**：host 开房间时给中继一个随机令牌，中继只存它的哈希。host 网络断开重连时出示同一个令牌；别人拿着配对码在这个空档里连上来会被拒绝。以前只要合法 host 的 socket 一断，拿到码的人就能顶替成 host，而加入方分辨不出来。
- **中继不再保存访问者地址**：限流计数原来把 IP 明文写进存储且从不删除，等于在积累一张永久的访客表。现在键是加盐摘要、盐按天轮换、早于当前一分钟的计数会被删掉。自建中继的内存计数同样清理，顺带修掉只增不减。
- **中继自己有了总量上限**：每个房间 64 MiB（两个方向都算），超了 429。8 MiB 的会话配额是 host 自律，拦不住不配合的客户端。
- **带密码的自建中继是全面私有的**：`/latest` 和 `/download/` 以前越过了鉴权，等于替人白跑 GitHub 流量。`agenthop update` 会从 `AGENTHOP_PASS` 读密码。
- 打开了中继的可观测性（日志里仍然只有时间和事件名，没有地址也没有配对码）。

## v0.3.3

- **自建中继修复**：host 连上后立刻发出的 `open` 帧可能被丢掉，于是它永远等不到 `ready`。中继现在在接受连接之前就把房间 id 算好，接受与挂监听之间不再有任何等待。托管的 Cloudflare 中继不受影响。
- 一场对话的两端各写各的日志：`<配对码>.create.log` 和 `<配对码>.join.log`。同机上两个 agent 共用家目录时，原先两端写进同一个文件，记录交织在一起。
- 启动后的第一行是日志的绝对路径（`local log <路径>`），不用再照着文档拼。

## v0.3.2

- 被拒绝的正文不再原样写进日志。拿到配对码的人本可以每分钟 60 次、每次约 2 MiB 文本灌满对方磁盘。现在只有对话本身完整记录。
- 开房的超时盖住整个过程（连接、发码、拿到房间），不再只盖住最后一步。中继收下连接后装死时，命令十五秒后明确报错，不再无限挂起。

## v0.3.1

- 拒绝发生在保存之前。v0.3.0 里第三个人的消息虽被记为 `peer refused`，附件却已经写进了 `~/.agenthop/inbox`。
- 会话有了用量上限：总量 8 MiB、2000 条消息、单条正文 64 KiB。
- 中继对同一个房间的写入限到每分钟 60 条，读取不计。
- 附件默认不写磁盘，只记名字（`peer files`）；要保存加 `--accept-files`。
- 附件上限从实际的约 72 KiB 恢复成文档写的 512 KiB（此前会撞上 SDK 的 body 解析上限，返回一段 HTML 的 413）。
- 密码可以用环境变量 `AGENTHOP_PASS`，不必写在命令行上。
- `update` 改成流式下载边写边校验，不再把整个程序读进内存。

## v0.3.0

- 连接断了用同一个配对码把房间接回来（最多四十五秒），两边写 `reconnecting` / `reconnected`，对话继续。
- 送不出去的话写成 `local undelivered <正文>`，不再无声消失；退出前把还没送出的也列出来。
- 一直没人加入而房间过期时写 `local expired`，不再报告一个从未存在的 `peer gone`。
- 加入方给自己的每一行签 id，第三个人拿着同一个配对码说话会被记成 `peer refused`。没有 id 的行仍然接受，兼容旧版本。
- 按 Ctrl-C 也会先把 bye 送出去再退出。
- 日志时间改成本机时间带时区偏移，不再是 UTC。
- `update` 校验 release 里的 `SHA256SUMS`：校验和从 GitHub 取、程序从中继取，对不上就丢弃，不替换现在能用的程序。
- `update` 不再把已经删掉的技能目录重新造出来。

## v0.2.3

- 告别是双向的：读到 bye 的一方会把 bye 说回去再退出，先开口的一方等这个回复，等不到就记 `peer gone`。

## v0.2.2

- **修复 `install` 会删掉自己**。从已安装的位置再执行一次 `install`，程序会把自己复制到自己身上，文件因此消失。v0.2.0 及更早在普通机器上就会发生，v0.2.1 在家目录经过符号链接时发生。

## v0.2.1

- `update` 换完程序后，用新程序把新的 `SKILL.md` 写回每一个记录过的目录。此前换了程序而技能文本仍是旧的，且没有任何提示。

## v0.2.0

- **不兼容**：去掉 `host`、`join`、`watch`、`send`、`reply`、`queue`、`inbox` 等命令和 `--agent`、`--on-receive`、`--ask` 等选项，打进去会给出迁移提示。
- **不兼容**：日志里 `connected` 的含义修正，创建方看到的是 `peer connected`。
- 配对码带大写或用空格隔开都能加入，不再被当成任务背景开一个新房间。
- 写错的选项不再变成 hello 发给对方。
- 新增 `/bye` 结束对话，`peer gone` 报告对方掉线。
- 从源码安装时装到 PATH 上的是启动器，不再是 node 本身。

## v0.1.10

- 要求对话过程出现在用户看得到的地方，另存一份时要给出路径和查看命令。

## v0.1.9

- 一次对话由同一个进程从头跑到尾：对方的话是它的标准输出，回复写进它的标准输入。
