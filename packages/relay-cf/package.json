{
  "name": "@agenthop/relay-cf",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest run --config vitest.config.ts && vitest run --config vitest.live.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@agenthop/tunnel": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250906.0",
    "@cloudflare/vitest-pool-workers": "^0.10.7",
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.136.1"
  }
}
