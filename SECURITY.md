# 安全说明

## 拿到配对码的人能做什么

这张表是这个工具的安全边界，先看它：

| | 能不能 |
|---|---|
| 作为对端加入，读到全部对话 | **能**——但要拿到**完整**的配对码，只有地址那半截不够 |
| 冒充对端 | 不能（v0.4.0 起）：每一句都要用配对码里的密钥封过 |
| 在 host 网络中断时顶替成 host | 不能（v0.3.4 起） |
| 拿中继当免费管道推流量 | 有上限（每房间 64 MiB、每分钟 60 次写入） |
| 中继运营者读到全部明文 | 不能（v0.4.0 起） |
| 中继运营者丢弃、延迟、重排对话 | **能**——加密挡不住这个 |

也就是说：**完整的配对码等于这次对话的全部权限**，只把它交给对面那个人，用完就作废（房间十分钟没有对话就消失）。

猜码不再是需要讨论的事：密钥那一段是 128 位随机值。地址那一段仍然只有约 2^44，但猜中它只能让人知道房间在哪，读不到一个字。

## 这一版保护什么，不保护什么

**配对码是一个地址加一把密钥。** 形如 `4821-amber-river-maple-k7f3q2mbxz4a6tu5wnhjy2pc3d`：前四段是房间地址，中继按它路由；最后一段是这次对话的密钥，**它从来不会发给中继**。两半都拿到才能进入房间，也才能读懂里面的话。房间在十分钟没有对话后消失——配对码是一次性的，不要当成长期密钥。

**中继读不到消息正文。** 正文在离开这台机器之前就用配对码里的密钥封好了（HKDF-SHA256 派生，AES-256-GCM，两个方向各一把密钥）。托管中继跑在 Cloudflare Workers 上、TLS 在 Cloudflare 终结，但它转发的是密文。中继仍然看得到：房间地址、消息条数、每条的大小和时间。

**房间的历史也是密文。** 读取房间队列的接口没有鉴权——任何知道房间地址的人都能把整段历史拉走，但拿到的每一行都是封好的。这就是这里不额外加一道读鉴权的原因：那道门只能用同一把密钥去锁，挡住的是同一批人，而挡不住真正要防的中继运营者。

**没有前向保密。** 密钥是配对码的确定函数。有人把整场流量录下来、事后又拿到了配对码，就能解开。配对码是一次性的、房间只活十分钟，但它确实出现在 `ps`、shell 历史和 agent 的会话记录里——创建方的日志文件第一行 `local waiting` 里也有完整的一份。

**附件的字节不加密。** 会话流程没有发送附件的入口，所以正常用法下不会有附件；协议层面若有，字节是明文走的，中继看得到。陌生人发来的附件现在在落盘之前就会被拒——拒绝发生在存储之前，不是之后。

**房间不会被顶替。** 第一个开房间的 host 会给中继一个随机令牌（中继只存它的 SHA-256）。它网络断了自动重连时出示同一个令牌，别人拿着地址在这个空档里连上来会被拒绝。

**中继不保存访问者地址。** 限流计数用的是加盐摘要而不是地址本身，盐每天轮换，早于当前一分钟的计数会被删掉。中继在任何时刻手里只有一分钟的计数。

**重放会被拒绝。** 每一句里都有一个只增不减的编号，收到重复编号的一句会被拒绝并写出 `peer refused 重复的消息`——不会安静地丢掉，也不会被当成新的一句。

**房间对拿到码的人做了限制。** 会话层只认第一个接入的对端。一次会话最多 8 MiB、2000 条消息、单条正文 64 KiB；中继对同一个房间的写入限到每分钟 60 条。附件默认不写磁盘，要写得显式加 `--accept-files`。

**更新会校验。** `agenthop update` 下载后比对 release 里的 `SHA256SUMS`，对不上就丢弃，不替换现在能用的程序。校验和优先从 GitHub 取、程序从中继取，这样单独换掉其中一方骗不过去；GitHub 取不到时会退回中继那一份，并在输出里说明这一点。

**密码不要写在命令行上。** 自建中继的 `--pass` 会出现在 `ps` 里，用环境变量 `AGENTHOP_PASS` 代替。设了密码的自建中继是全面私有的，`/latest` 和 `/download/` 也要带密码——`agenthop update` 会自动从 `AGENTHOP_PASS` 读。配对码同样会出现在 `ps` 里，这是它目前的传递方式。

## 报告问题

发现安全问题请开一个 [GitHub Issue](https://github.com/sdyuyouth/agenthop/issues)；涉及尚未公开的漏洞细节时，请用 GitHub 的 [Security Advisory](https://github.com/sdyuyouth/agenthop/security/advisories/new) 私下提交。

## 支持的版本

只有最新发布的版本会收到修复。v0.3.x 及更早的版本没有端到端加密，中继能读到对话正文，请 `agenthop update` 升级；v0.2.1 及更早的版本还有一个会删掉自己的 `install` 缺陷。
